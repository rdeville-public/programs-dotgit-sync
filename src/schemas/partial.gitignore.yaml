---
schema;gitignore:
  map:
    query:
      seq:
        - type: str
    templates:
      seq:
        - type: str
          enum:
            - base
            - python
            - javascript
            - jsonnet
